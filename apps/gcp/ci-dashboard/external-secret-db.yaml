apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ci-dashboard-eq-prd-insight-db
  namespace: apps
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: ee-gcp-sm
    kind: ClusterSecretStore
  target:
    name: ci-dashboard-eq-prd-insight-db
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: gcp_ci_dashboard_eq_prd_insight_db
