apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github
spec:
  secretStoreRef:
    name: ee-gcp-sm
    kind: ClusterSecretStore
  target:
    name: github
  data:
    - secretKey: token
      remoteRef:
        key: github-bot-token
