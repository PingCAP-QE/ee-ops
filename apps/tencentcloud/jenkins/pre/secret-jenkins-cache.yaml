apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jenkins-cache
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: ee-gcp-sm
    kind: ClusterSecretStore
  target:
    name: jenkins-cache
    creationPolicy: Owner
    template:
      type: Opaque
      engineVersion: v2
      data:
        region: "{{ .region }}"
        bucket: "" # do not set bucket when the endpoint is virtual-hosted-style.
        endpoint: 'https://{{ .bucket}}.cos.{{ .region }}.myqcloud.com'
        access-key: "{{ .accessKey }}"
        access-secret: "{{ .accessSecret }}"
  dataFrom:
    - extract:
        key: "${JENKINS_CACHE_S3_JSON}" # json object containing keys: `region`, `bucket`, and `endpoint`
    - extract:
        key: "${S3_CREDENTIALS_JSON}" # json object containing keys: `accessKey` and `accessSecret`
