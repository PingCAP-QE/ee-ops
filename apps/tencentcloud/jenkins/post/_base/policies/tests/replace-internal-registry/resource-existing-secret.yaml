apiVersion: v1
kind: Pod
metadata:
  name: pod-existing-secret
  namespace: jenkins-tidb
  labels:
    kubernetes.jenkins.io/controller: ""
spec:
  containers:
    - name: utils
      image: us-docker.pkg.dev/pingcap-testing-account/internal/test/jenkins/centos7:cached
  imagePullSecrets:
    - name: custom-registry-auth
