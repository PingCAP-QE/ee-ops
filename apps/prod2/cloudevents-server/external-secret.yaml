apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudevents-server-config
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: ee-gcp-sm
  target:
    name: cloudevents-server-config
    creationPolicy: Owner
  data:
    - secretKey: config.yaml
      remoteRef:
        key: prod2_cloudevents-server-config-yaml
