apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: brc-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1.1Ti
  storageClassName: openebs-single-replica
  volumeMode: Filesystem
