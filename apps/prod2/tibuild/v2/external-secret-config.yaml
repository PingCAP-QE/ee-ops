apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: v2-config
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: ee-gcp-sm
  target:
    name: v2-config
    creationPolicy: Owner
  data:
    - secretKey: config.yaml
      remoteRef:
        key: prod2_tibuild_v2_config_yaml
