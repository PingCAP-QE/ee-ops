presubmits:
  PingCAP-QE/ee-ops:
    - name: verifyCI-k8s-generic
      always_run: true
      max_concurrency: 2
      spec:
        containers:
          - image: bitnami/kubectl
            args:
              - --namespace
              - apps
              - get
              - pods
            # FIXME:
            # unable to merge value from key ''job2'' in ConfigMap ''apps/prow-job-config-hdk74thb2b'' into target path ''prow.jobs.job2'': key " \"apps\"" has no value (cannot end with ,)
            # can not use this inline yaml format:
            # command: ["--namespace","apps","get","pods"]
