presubmits:
  PingCAP-QE/ee-ops:
    - name: verifyCI-k8s-generic
      always_run: true
      max_concurrency: 2
      spec:
        containers:
          - image: bitnami/kubectl
            args:
              - --namespace
              - apps
              - get
              - pods
