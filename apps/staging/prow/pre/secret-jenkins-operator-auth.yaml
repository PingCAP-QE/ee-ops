apiVersion: v1
kind: Secret
metadata:
  name: prow-jenkins-operator-auth
  namespace: apps
type: Opaque
stringData:
  user: "${BUCKET_REGION}" # jenkins auth username.
  # TODO(wuhuizuo): how to prepare it automatic.
  token: "${BUCKET_NAME}" # jenkins user api token, not password.
