apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: prow-openai
    namespace: apps
    behavior: replace
    options:
      disableNameSuffixHash: true
    files:
      - chatgpt/tasks.yaml
