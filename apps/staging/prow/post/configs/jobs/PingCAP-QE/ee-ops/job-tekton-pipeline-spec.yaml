presubmits:
  PingCAP-QE/ee-ops:
    - name: verifyCI2
      always_run: true
      max_concurrency: 2
      decoration_config: null
      agent: tekton-pipeline
      pipeline_run_spec:
        pipelineSpec:
          tasks:
            - name: taskName
              taskSpec:
                steps:
                  - name: build-sources
                    image: ubuntu
                    command:
                      - /bin/bash
                    args:
                      - -c
                      - |
                        echo hello world
                        pwd
                        ls