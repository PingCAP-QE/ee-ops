apiVersion: v1
kind: Secret
metadata:
  name: bazel
type: Opaque
stringData:
  bazel: |
    build:ci --remote_cache=http://brc-bazel-remote.apps.svc --remote_timeout="10s"
    test:ci --remote_cache=http://brc-bazel-remote.apps.svc --remote_timeout="10s"
