apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - setup.yaml
  - configs.yaml
  # FIXME: currently can not assign a same IP address to multi Ingress resources in GKE.
  # - ingress.yaml