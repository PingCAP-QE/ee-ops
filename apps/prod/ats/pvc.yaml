---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ats-storage
  namespace: apps
spec:
  resources:
    requests:
      storage: 1100Gi
  storageClassName: ceph-block
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce