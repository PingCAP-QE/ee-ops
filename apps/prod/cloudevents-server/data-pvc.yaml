apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloudevents-server-data
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
