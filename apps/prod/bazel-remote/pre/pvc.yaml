---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: brc-cache-rbd
  namespace: apps
spec:
  resources:
    requests:
      storage: 1100Gi
  storageClassName: ceph-block
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce