apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
- pvc.yaml
- redis-cluster.yaml
- server.yaml
- shard-worker.yaml
configMapGenerator:
  - name: buildfarm
    options:
      disableNameSuffixHash: true
    files:
      - configs/config.yaml
