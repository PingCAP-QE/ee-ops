---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buildfarn
  namespace: apps
spec:
  resources:
    requests:
      storage: 100Gi
  storageClassName: ceph-filesystem
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
