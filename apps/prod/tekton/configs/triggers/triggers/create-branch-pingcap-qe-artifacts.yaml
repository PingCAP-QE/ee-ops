apiVersion: triggers.tekton.dev/v1beta1
kind: Trigger
metadata:
  name: branch-create-pingcap-qe-artifacts
  labels:
    type: github-branch-create
    github-owner: PingCAP-QE
    github-repo: artifacts
spec:
  interceptors:
    - name: filter on repo owner and name
      ref:
        name: cel
      params:
        - name: filter
          value: >-
            body.repository.owner.login == 'PingCAP-QE' && body.repository.name ==
            'artifacts'
  bindings:
    - ref: github-branch-create
  template:
    ref: branch-create-pingcap-qe-artifacts
