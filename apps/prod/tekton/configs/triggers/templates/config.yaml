apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: tekton-config-triggers-templates
spec:
  interval: 1m0s
  sourceRef:
    kind: GitRepository
    name: ci
  path: ./tekton/v0/triggers/templates
  prune: true
