apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cargo-home
spec:
  resources:
    requests:
      storage: 100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
