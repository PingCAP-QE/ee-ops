{{- if and (eq .Values.paperclip.deploymentMode "authenticated") (not .Values.auth.existingSecret) -}}
{{- $secretName := include "paperclip.authSecretName" . -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- $secretKey := .Values.auth.secretKey -}}
{{- $existingData := dict -}}
{{- if and $existing (kindIs "map" $existing) -}}
{{- $existingData = (get $existing "data" | default dict) -}}
{{- end -}}
{{- $secretValue := "" -}}
{{- if .Values.auth.secretValue -}}
{{- $secretValue = .Values.auth.secretValue -}}
{{- else if hasKey $existingData $secretKey -}}
{{- $secretValue = (index $existingData $secretKey | b64dec) -}}
{{- else -}}
{{- $secretValue = randAlphaNum 48 -}}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "paperclip.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ $secretKey }}: {{ $secretValue | quote }}
{{- end }}
