apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-server-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: openebs-3-replicas
