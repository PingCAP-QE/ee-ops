---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: openebs
  namespace: openebs
spec:
  releaseName: openebs
  chart:
    spec:
      chart: openebs
      sourceRef:
        kind: HelmRepository
        name: openebs
        namespace: openebs
      version: "4.2.0"
  interval: 10m0s
  install:
    remediation:
      retries: 3
  values:
    engines:
      local:
        lvm:
          enabled: false
        zfs:
          enabled: false
      replicated:
        mayastor:
          enabled: false
    localpv-provisioner:
      localpv:
        basePath: /data/openebs/local
    lvm-localpv:
      crds:
        lvmLocalPv:
          enabled: false
      lvmNode:
        kubeletDir: /data/kubelet
    zfs-localpv:
      crds:
        zfsLocalPv:
          enabled: false
      zfsNode:
        kubeletDir: /data/kubelet
    openebs-crds:
      csi:
        volumeSnapshots:
          enabled: false
